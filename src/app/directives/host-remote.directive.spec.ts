import { HostRemoteDirective } from './host-remote.directive';

describe('HostRemoteDirective', () => {
  it('should create an instance', () => {
    const directive = new HostRemoteDirective();
    expect(directive).toBeTruthy();
  });
});
